[project]
name = "kubemq-mcp-python-examples"
version = "1.0.0"
description = "KubeMQ MCP Python examples"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0",
    "httpx",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = []
